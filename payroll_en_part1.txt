<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>MNU HR Lab — Payroll Calculation Trainer</title>
<style>
  :root{
    --bg:#f5f3ee;--paper:#fffdf8;--ink:#171717;--muted:#6c6a66;--line:#dcd8cf;
    --accent:#e8ff62;--accent2:#bda7ff;--good:#d7f5df;--bad:#ffd8d8;--warn:#fff0bd;
    --shadow:0 18px 50px rgba(29,27,23,.10);--radius:24px;
  }
  *{box-sizing:border-box}
  html{scroll-behavior:smooth}
  body{margin:0;background:var(--bg);color:var(--ink);font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}
  button,input{font:inherit}
  button{cursor:pointer}
  .app{min-height:100vh}
  .topbar{position:sticky;top:0;z-index:30;background:rgba(245,243,238,.90);backdrop-filter:blur(14px);border-bottom:1px solid var(--line)}
  .topbar-inner{max-width:1180px;margin:auto;padding:14px 22px;display:flex;gap:16px;align-items:center;justify-content:space-between}
  .brand{font-size:12px;font-weight:800;letter-spacing:.12em;text-transform:uppercase}
  .top-actions{display:flex;gap:10px;align-items:center}
  .pill{border:1px solid var(--ink);border-radius:999px;padding:8px 12px;font-size:12px;font-weight:700;background:transparent}
  .score-pill{background:var(--ink);color:white;border-color:var(--ink)}
  main{max-width:1180px;margin:auto;padding:34px 22px 70px}
  .hero{display:grid;grid-template-columns:1.25fr .75fr;gap:24px;align-items:stretch}
  .hero-main,.hero-side,.card{background:var(--paper);border:1px solid var(--line);border-radius:var(--radius);box-shadow:var(--shadow)}
  .hero-main{padding:44px}
  .eyebrow{font-size:12px;letter-spacing:.14em;text-transform:uppercase;font-weight:800;color:var(--muted);margin-bottom:20px}
  h1{font-size:clamp(42px,6vw,78px);line-height:.94;letter-spacing:-.055em;margin:0 0 24px;max-width:900px}
  .lead{font-size:20px;line-height:1.45;max-width:720px;margin:0;color:#35332f}
  .hero-side{padding:28px;display:flex;flex-direction:column;justify-content:space-between;gap:24px;background:var(--ink);color:white}
  .outcome-label{font-size:11px;letter-spacing:.14em;text-transform:uppercase;color:#c6c3bc;font-weight:800}
  .outcome{font-size:28px;line-height:1.1;font-weight:800;letter-spacing:-.03em;margin-top:8px}
  .steps-mini{display:grid;gap:9px}
  .steps-mini div{display:flex;gap:10px;align-items:center;font-size:14px;color:#ece8df}
  .num{width:26px;height:26px;border-radius:50%;display:grid;place-items:center;background:var(--accent);color:var(--ink);font-size:11px;font-weight:900}
  .primary{border:0;background:var(--accent);color:var(--ink);font-weight:900;border-radius:15px;padding:16px 20px;box-shadow:0 6px 0 #111;transition:.15s transform,.15s box-shadow}
  .primary:hover{transform:translateY(2px);box-shadow:0 4px 0 #111}
  .secondary{border:1px solid var(--ink);background:transparent;color:var(--ink);font-weight:800;border-radius:14px;padding:13px 16px}
  .ghost{border:1px solid var(--line);background:var(--paper);color:var(--ink);font-weight:750;border-radius:14px;padding:11px 14px}
  .hidden{display:none!important}

  .lab{margin-top:28px}
  .progress-wrap{background:var(--paper);border:1px solid var(--line);border-radius:20px;padding:16px 18px;margin-bottom:18px;box-shadow:var(--shadow)}
  .progress-meta{display:flex;justify-content:space-between;gap:20px;font-size:12px;font-weight:800;text-transform:uppercase;letter-spacing:.08em;margin-bottom:10px}
  .progress{height:10px;border-radius:999px;background:#e5e1d8;overflow:hidden}
  .progress > div{height:100%;background:var(--ink);width:0%;transition:.35s width}
  .layout{display:grid;grid-template-columns:330px minmax(0,1fr);gap:18px;align-items:start}
  .sidebar{position:sticky;top:86px;display:grid;gap:16px}
  .card{padding:24px}
  .case-card{background:var(--ink);color:white}
  .case-role{font-size:28px;font-weight:900;letter-spacing:-.035em;line-height:1.05;margin:8px 0}
  .case-sub{color:#bbb7ae;font-size:14px;margin-bottom:22px}
  .salary{font-size:32px;font-weight:900;background:var(--accent);color:var(--ink);padding:14px 16px;border-radius:16px;display:inline-block}
  .small{font-size:12px;color:var(--muted);line-height:1.45}
  .case-card .small{color:#bbb7ae}
  .rates{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:14px}
  .rate{border:1px solid var(--line);border-radius:12px;padding:10px;background:#fff}
  .rate b{display:block;font-size:13px}.rate span{font-size:12px;color:var(--muted)}
  .stage-title{display:flex;gap:14px;align-items:flex-start;margin-bottom:18px}
  .stage-index{min-width:42px;height:42px;border-radius:13px;background:var(--accent2);display:grid;place-items:center;font-weight:900}
  h2{font-size:30px;line-height:1.05;letter-spacing:-.035em;margin:1px 0 5px}
  .stage-desc{color:var(--muted);font-size:14px}
  .question{border-top:1px solid var(--line);padding:20px 0}
  .question:first-of-type{border-top:0}
  .q-head{display:flex;justify-content:space-between;gap:16px;align-items:flex-start;margin-bottom:10px}
  .q-title{font-weight:850;font-size:17px}
  .formula{font-family:"SFMono-Regular",Consolas,monospace;font-size:12px;color:#56524c;background:#f1eee6;padding:9px 11px;border-radius:10px;margin:8px 0 12px;display:none}
  .guided .formula{display:block}
  .input-row{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:10px}
  .answer{width:100%;border:1.5px solid #bdb8ad;border-radius:14px;padding:14px 15px;background:white;font-size:18px;font-weight:750;outline:none}
  .answer:focus{border-color:var(--ink);box-shadow:0 0 0 3px rgba(23,23,23,.08)}
  .check{border:0;border-radius:13px;background:var(--ink);color:white;font-weight:800;padding:0 18px}
  .feedback{margin-top:10px;border-radius:12px;padding:10px 12px;font-size:13px;font-weight:650;display:none}
  .feedback.good{display:block;background:var(--good)}
  .feedback.bad{display:block;background:var(--bad)}
  .actions{display:flex;gap:10px;justify-content:space-between;align-items:center;border-top:1px solid var(--line);padding-top:20px;margin-top:8px}
  .stage{display:none}.stage.active{display:block}
  .mode-tabs{display:flex;gap:8px;background:#ebe7de;padding:5px;border-radius:14px}
  .mode-tabs button{border:0;background:transparent;padding:8px 10px;border-radius:10px;font-weight:800;font-size:12px}
  .mode-tabs button.active{background:white;box-shadow:0 2px 10px rgba(0,0,0,.08)}
  .result-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-top:18px}
  .metric{background:#f2efe7;border-radius:16px;padding:16px}
  .metric.emphasis{background:var(--accent)}
  .metric span{display:block;font-size:12px;color:#625e57;text-transform:uppercase;letter-spacing:.06em;font-weight:800}
  .metric b{font-size:26px;letter-spacing:-.025em;display:block;margin-top:5px}
  .breakdown{margin-top:18px;border:1px solid var(--line);border-radius:16px;overflow:hidden}
  .row{display:grid;grid-template-columns:1fr auto;gap:14px;padding:12px 14px;border-top:1px solid var(--line);font-size:14px}.row:first-child{border-top:0}.row b{font-variant-numeric:tabular-nums}
  .certificate{padding:22px;border:2px solid var(--ink);border-radius:18px;margin-top:20px;display:flex;justify-content:space-between;gap:20px;align-items:center}
  .bigscore{font-size:50px;font-weight:950;letter-spacing:-.06em}
  .toast{position:fixed;right:20px;bottom:20px;background:var(--ink);color:white;padding:12px 15px;border-radius:12px;box-shadow:var(--shadow);z-index:50;display:none}.toast.show{display:block}
  .note{background:var(--warn);border-radius:14px;padding:13px 14px;font-size:12px;line-height:1.45;margin-top:14px}

  @media(max-width:900px){
    .hero,.layout{grid-template-columns:1fr}.sidebar{position:static}.hero-main{padding:30px}.hero-side{min-height:340px}h1{font-size:52px}.result-grid{grid-template-columns:1fr}.topbar-inner{padding:12px 14px}.brand{max-width:190px}.pill.hide-mobile{display:none}}
  @media(max-width:520px){main{padding:20px 12px 50px}.card{padding:18px}.hero-main{padding:24px}.input-row{grid-template-columns:1fr}.check{padding:12px}.rates{grid-template-columns:1fr}.actions{flex-wrap:wrap}.salary{font-size:27px}.certificate{align-items:flex-start;flex-direction:column}}
</style>
</head>
<body>
